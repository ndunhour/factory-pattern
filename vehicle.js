function vehicleType(car, truck, minivan) {
  vehicleType.make = "Toyota";
}
  //create the class of car
  var vehicle = {
    make: 'make',
    model: 'model',
    year: 1998
  };

  console.log(vehicle.make);

  console.log(vehicleType(vehicle));

